---
name: computer-network-notes
description: 整理家俊的「湖科大计网」笔记（湖南科技大学高军老师《计算机网络》MOOC，Obsidian vault）。当用户说「整理湖科大计网 2.1」「整理计网 2.4」「分别整理这几节」时使用。覆盖课件定位、PDF 渲染看图、关键图导出命名、笔记排版规范、多节并行分工。
agent_created: true
---

# 湖科大计网笔记整理

## 0. 全局约定（必读）

- 笔记库：`/Users/tujiajun/Desktop/ob仓库/Note`
- 笔记目标：`计算机网络/笔记/湖科大/第X章/N-M 节标题.md`
- 图片目标：`计算机网络/笔记/湖科大/第X章/assets/N-M 节标题/`
- 课件源：`计算机网络/湖科大计网/第X章 …/X.Y 节标题.pdf`（**只读，不要修改**）
- 教材参考：同目录 `深入浅出计算机网络（高军）.pdf`（同为高军老师，术语可对齐，但**以课件为准**）
- **权威规范是 `计算机网络/AGENTS.md`（91 行）。动手前先读它**，本技能只补充工具链与实操细节。

目录名用阿拉伯数字（`第2章`），图片名用中文数字（`第二章-P0NN-主题.png`）—— 这是第 1 章已确立的既有风格，照抄别改。

## 1. 解析用户指令

「整理湖科大计网 2.1」= 第 2 章 2.1 节。先 `ls 湖科大计网/第X章 …/` 用节号前缀匹配 PDF。

**一节 = 一篇笔记**（`N-M 节标题.md`）。用户一次给多节（如「分别整理 2.1 2.2 2.3」）→ 每节独立一篇，**并行处理**。

## 2. 提取课件（工具链）

```bash
PY=/Users/tujiajun/.workbuddy/binaries/python/envs/default/bin/python
# pymupdf / pillow 已装好，不要用系统 python，不要 pip install 全局
```

**关键认知：湖科大课件的文字层极其稀疏**——正文几乎全在图片里（`get_text()` 只能拿到页眉、页码、零散标签）。**必须以看图为准**，文字层只能当辅助索引。

```python
import pymupdf
d = pymupdf.open(pdf); m = pymupdf.Matrix(144/72, 144/72)   # → 1920x1080
for i, pg in enumerate(d):
    pg.get_pixmap(matrix=m).save(f"/tmp/jw/render/{节名}/P{i+1:03d}.png")
```

渲染到临时目录（如 `/tmp/jw/render/<节名>/`），用 Read 逐页看图判断。

**跳坑提示（重要）**：
- 同一 session 里 Read 已读过的图片会返回「Image unchanged」而看不到内容。**要复核某页时，先用 pillow 缩放另存成新文件名/jpg 再 Read**。
- 每页页脚有大量「中国大学MOOC」水印，忽略。
- 有些页是 **PPT 内嵌视频的占位黑框**（如第 2 章 P029 激光、P030 可见光），框内无内容、无文字要点。判定方法：pillow 算暗度/方差（`avg≈140, var>10000` 即为暗色视频框）。这类页**不导出、不插图**，在笔记里一句话注明"课件此处为视频演示"。
- 课件后段常有前半段的**复习重复页**（同一张图再放一遍），只导一次，笔记里用 `> [!note]- 知识点回顾` callout 挂图。

## 3. 插图判断（AGENTS.md 的核心要求）

- ✅ 插：网络拓扑图、协议栈图、帧/数据报格式图、转发流程图、时序图、状态图、结构剖面图（双绞线/光纤/光缆）、原理图（全反射）、示意图、**知识点总览/回顾图**、真题页。
- ❌ 不插：纯文字页、纯定义页、卡通类比页、**纯表格页**（转写成 Markdown 表格）。
- 表格页判定：页面主体是表格 → 转文字表格，不插图。参数类（带宽、波长、速率距离、频段用途）必须完整保留数字。

**文件命名**：`第一章-P005-物联网连接示意图.png` 格式，即 `<章的中文数字>-P<PDF页码3位>-<主题>.png`。用 `cp` 从渲染目录复制过去，**整页导出、不裁剪**。

笔记里引用用裸内链、不带路径：`![[第二章-P011-光纤结构.png]]`。

## 4. 笔记结构

严格照抄同章已完成的姊妹篇（如 `第1章/1-2 因特网概述.md`、`第2章/2-3 传输方式.md`）：

```markdown
---
title: 物理层概述
aliases:
  - 2.1 物理层概述
tags:
  - 计算机网络
  - 湖科大计网
  - 第2章
source: 湖南科技大学《计算机网络》2.1 物理层概述
---

# 2.1 物理层概述

## 一、…（二级标题用「一、二、三」）
### 1. …（三级标题用阿拉伯数字）
```

- 重点用 `<font color="#ff0000">…</font>`，**克制使用**（术语首现、关键结论）。
- callout：`[!note]` 易混/补充、`[!success]-` 真题答案解析（可折叠）、`[!summary]` 本节要点、`[!note]-` 知识点回顾挂图。
- 课件里的 **408 真题必须完整保留**（题干、选项、答案、解析）。
- 内容以**课件为主**：只做转写 + 排版性归纳，**不扩写背景、不新增考点**。术语用课件原话（例：课件说「传输媒体」就不要写成「传输介质」）。
- 结尾按样板收一个简短的对比/总结表格，但课件本身没有则不强加。

## 5. 多节并行分工

用户一次要 3 节以上时，**每节派一个 general-purpose 子 agent 并行**，各自负责「看该节全部渲染页 → cp 导出关键图 → 写 md」。派活时 prompt 必须写全：

1. 课件 PDF 绝对路径 + 页数；已渲染页目录 + 命名（`P001.png` 起）；
2. 必须先读 `计算机网络/AGENTS.md` 和一篇**同章已完成姊妹篇**当风格样板（点名具体文件）；
3. 输出：笔记绝对路径 + assets 目录 + 图片命名规则 + 用 `cp` 整页导出；
4. 该节的已知要点（从文字层和课件目录先自己读一遍再写进 prompt，供 agent 核对，避免跑偏）；
5. 插图/不插图判定规则；
6. 要求 agent 返回：文件路径、导出图清单、判为纯文字未插图的页、异常页。

**注意**：子 agent 有被中断的风险（文件已导出但 md 未落盘）。派完活必须**回查落盘情况**（`find 第X章 -type f`），缺哪节就单独补一个 agent 只写正文。

## 6. 收尾自查

1. 逐篇核对引用与文件一一对应：
   ```bash
   grep -o '!\[\[[^]]*\]\]' 笔记.md | sed 's/!\[\[//;s/\]\]//' | sort > /tmp/ref.txt
   ls assets/<节名>/ | sort > /tmp/have.txt
   comm -23 /tmp/ref.txt /tmp/have.txt   # 引用了但没有文件的
   comm -13 /tmp/ref.txt /tmp/have.txt   # 有文件但没引用的
   ```
2. 对照 AGENTS.md：关键图是否都插了？术语是否用课件原话？
3. 写记忆日志：`/Users/tujiajun/WorkBuddy/<session>/.workbuddy/memory/YYYY-MM-DD.md`。
4. 用 `present_files` 展示生成的笔记。

## 7. 课程进度

**不要依赖本节硬编码——动手前先 `ls` 笔记目录与课件目录自行判断该节是否已做过。**

截至 2026-09-16（已与磁盘核对）：

- 第 1 章：笔记完成 1-1 ~ 1-6（只有 1-1/1-2/1-3 有 assets）。
- 第 2 章：已完成 **2-1 物理层概述**（6 图）、**2-2 物理层下面的传输媒体**（18 图）、**2-3 传输方式**（6 图）。**2.4 编码与调制、2.5 信道的极限容量、2.6 信道复用技术尚未整理。**
- 课件 `湖科大计网/` 下已有第 1、2、3、5、6 章的节级 PDF（**第 4 章网络层还没有**）。
